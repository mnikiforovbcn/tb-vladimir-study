% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cascade.R
\name{.proportion_ci}
\alias{.proportion_ci}
\title{Proportion-with-Wilson-CI summary of a logical flag, optionally grouped}
\usage{
.proportion_ci(df, flag, group_vars = character(0))
}
\arguments{
\item{df}{A data frame, used for \code{group_vars}.}

\item{flag}{Either the name of a logical/0-1 column in \code{df}, or a
logical vector of length \code{nrow(df)}.}

\item{group_vars}{Character vector of column names in \code{df} to group by;
\code{character(0)} (the default) summarises the whole table as one row.}
}
\value{
A tibble with the \code{group_vars} columns, \code{n}, \code{x}.
}
\description{
\code{NA} in \code{flag} means "unknown" and is excluded from both the numerator
and the denominator -- consistent with how \code{\link[=cascade_flags]{cascade_flags()}} propagates
missingness through the pipeline.
}
\keyword{internal}
