% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{load_raw}
\alias{load_raw}
\title{Load the raw VladKovMur_dataset.csv export}
\usage{
load_raw(path = raw_data_path())
}
\arguments{
\item{path}{Path to the CSV file. Defaults to \code{\link[=raw_data_path]{raw_data_path()}}.}
}
\value{
A tibble with \link{N_COLS_EXPECTED} columns.
}
\description{
Reads with an explicit column-type map (see \code{\link[=raw_col_types]{raw_col_types()}}), verifies
the column layout exactly matches the documented data dictionary, and
casts every flag/code/count column to integer. Row count is deliberately
\emph{not} asserted here -- the dataset grows with each new export; pin row
count in a test against a specific snapshot instead, not in this
function.
}
