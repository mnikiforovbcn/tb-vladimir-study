% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc.R
\name{audit_missingness}
\alias{audit_missingness}
\title{Missingness audit, by column and by site}
\usage{
audit_missingness(df)
}
\arguments{
\item{df}{A data frame as returned by \code{\link[=load_raw]{load_raw()}}.}
}
\value{
A list with \code{by_column} (overall missingness per column) and
\code{by_site} (missingness per column per \code{Source}).
}
\description{
Reports \code{NA} counts and rates for every column, both overall and broken
down by \code{Source}. Columns in \link{.ALWAYS_REQUIRED_COLUMNS} are marked
\code{"unexplained"} when missing (these should be populated for every
enrolled record); all other missingness is marked \code{"structural"}, since
the cascade's skip pattern (e.g. no outcome date until treatment ends)
produces it by design.
}
