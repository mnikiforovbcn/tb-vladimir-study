% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc.R
\name{check_dose_consistency}
\alias{check_dose_consistency}
\title{Flag dose-count anomalies and threshold-flag inconsistencies}
\usage{
check_dose_consistency(df)
}
\arguments{
\item{df}{A data frame as returned by \code{\link[=load_raw]{load_raw()}}.}
}
\value{
A tibble of flagged rows with an \code{issue} column (a row may appear
more than once if multiple issues apply).
}
\description{
Detects three issues, labelled via \code{issue}: \code{"doses_exceed_schema"}
(\code{DosesTaken > SchemaDoses}), \code{"take50pc_inconsistent"} (\code{Take50pc} does
not match whether the dose ratio reached 50\%), and
\code{"take100pc_inconsistent"} (likewise for \code{Take100pc}). Rows with
\code{SchemaDoses} missing or zero are excluded from the threshold checks
(the ratio is undefined) but still checked for \code{doses_exceed_schema}.
}
