% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cascade.R
\name{.incentive_step}
\alias{.incentive_step}
\title{Uptake proportion and median payment delay for a single incentive}
\usage{
.incentive_step(
  df,
  flag_col = NULL,
  flag_vec = NULL,
  date_col,
  milestone_col,
  label,
  group_vars = character(0)
)
}
\arguments{
\item{df}{A data frame already restricted to those eligible for this incentive.}

\item{flag_col}{Character: name of the 0/1 "received" column, or \code{NULL}
if \code{flag_vec} is supplied directly (for the TreatGroup-dispatched 1-year incentive).}

\item{flag_vec}{Optional logical vector, used instead of \code{flag_col}.}

\item{date_col}{Character: name of the payment-date column (or a
computed column already present in \code{df}).}

\item{milestone_col}{Character: name of the reference-date column the
delay is measured from.}

\item{label}{Character: value for the \code{milestone} column in the result.}

\item{group_vars}{Character vector of column names to group by.}
}
\value{
A tibble: \code{milestone}, \code{group_vars}, \code{n}, \code{x}, \code{proportion},
\code{ci_lower}, \code{ci_upper}, \code{median_delay_days}, \code{delay_iqr_low},
\code{delay_iqr_high}, \code{suppressed}.
}
\description{
Uptake proportion and median payment delay for a single incentive
}
\keyword{internal}
