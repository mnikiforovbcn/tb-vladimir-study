% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz.R
\name{.ensure_pandoc}
\alias{.ensure_pandoc}
\title{Point \code{RSTUDIO_PANDOC} at Quarto's bundled pandoc if no other pandoc is on PATH}
\usage{
.ensure_pandoc()
}
\value{
\code{TRUE} if pandoc is available (so \code{selfcontained = TRUE} is safe).
}
\description{
\code{\link[htmlwidgets:saveWidget]{htmlwidgets::saveWidget()}}'s \code{selfcontained = TRUE} requires pandoc;
this machine has no standalone pandoc install, only the copy bundled
inside the Quarto install (Phase 0). Falls back to \code{FALSE} (a
non-self-contained HTML plus a sibling \verb{_files/} directory) if no
pandoc can be found at all, rather than erroring.
}
\keyword{internal}
