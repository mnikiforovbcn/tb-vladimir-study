% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cascade.R
\name{suppress_small_cells}
\alias{suppress_small_cells}
\title{Suppress small cells per the Study Plan's disclosure-control policy}
\usage{
suppress_small_cells(df, count_cols, threshold = SMALL_CELL_THRESHOLD)
}
\arguments{
\item{df}{A data frame, one row per stratified cell.}

\item{count_cols}{Character vector of column names in \code{df} holding raw
counts to check against \code{threshold} (typically the denominator \code{n}
and, where present, the numerator \code{x}).}

\item{threshold}{Minimum count required to display a cell; defaults to
\link{SMALL_CELL_THRESHOLD}.}
}
\value{
\code{df} with a new \code{suppressed} logical column, and all numeric
columns blanked to \code{NA} on suppressed rows.
}
\description{
Per Descriptive Study Plan SS11, no stratified cell with a count below
\code{threshold} may be presented, since it could indirectly identify an
individual (particularly at the smaller sites). Any row where a column
in \code{count_cols} is below \code{threshold} has every numeric column (counts,
proportions, confidence intervals) replaced with \code{NA} and is marked
\code{suppressed = TRUE}, so a caller can render e.g. \code{"<5"} without ever
holding the real value past this point in the pipeline.
}
