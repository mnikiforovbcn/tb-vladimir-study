% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cascade.R
\name{incentive_uptake}
\alias{incentive_uptake}
\title{Step 7 incentive payment uptake among those eligible, with median payment delay}
\usage{
incentive_uptake(df, group_vars = character(0))
}
\arguments{
\item{df}{Analysis-ready table.}

\item{group_vars}{Character vector of column names to group by.}
}
\value{
A tibble: \code{milestone} (one row per incentive) plus the
\code{\link[=.incentive_step]{.incentive_step()}} columns, suppressed per \code{\link[=suppress_small_cells]{suppress_small_cells()}}.
}
\description{
Delay is measured from the incentive's associated programmatic milestone
to the payment date: \code{DateScreening} for the screening incentive, and
\code{DatePrevTreatmentStart} for the dose-threshold and 1-year incentives --
the dataset records only the payment date for those, not the exact date
the threshold was crossed, so treatment start is the nearest available
reference point. The 1-year incentive's flag/date columns are dispatched
by \code{TreatGroup}, matching \code{\link[=cascade_flags]{cascade_flags()}}'s \code{supp_1yr_received}.
}
