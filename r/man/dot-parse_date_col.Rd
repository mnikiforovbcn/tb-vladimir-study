% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{.parse_date_col}
\alias{.parse_date_col}
\title{Parse a date column tolerant of a trailing time-of-day component}
\usage{
.parse_date_col(x)
}
\arguments{
\item{x}{Character vector.}
}
\value{
A \code{Date} vector.
}
\description{
Truncates to the first 10 characters (\code{YYYY-MM-DD}) before parsing, so
\code{"2020-05-14 00:00:00"} parses the same as \code{"2020-05-14"}. A string that is
still unparseable after truncation (e.g. a truncated year like
\code{"201-08-01"}) becomes \code{NA}, same as readr's own coercion behaviour, and is
reported by \code{\link[=load_raw]{load_raw()}} rather than silently swallowed.
}
\details{
Strings are required to match \code{YYYY-MM-DD} exactly (4-digit year) before
being handed to \code{\link[=as.Date]{as.Date()}}: R's date parser otherwise accepts a
short/truncated year (e.g. \code{"201-08-01"} parses as year 201 rather than
failing), which would silently mask a real data-entry error as a valid
date far in the past.
}
\keyword{internal}
