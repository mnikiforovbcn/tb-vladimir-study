% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trends.R
\name{enrollment_trends}
\alias{enrollment_trends}
\title{Step 9 enrollment, treatment-initiation, and outcome trends in one tidy table}
\usage{
enrollment_trends(df, group_vars = character(0))
}
\arguments{
\item{df}{Analysis-ready table.}

\item{group_vars}{Character vector of additional grouping columns.}
}
\value{
A tibble: \code{milestone} (\code{"Enrollment"}, \code{"Treatment initiation"},
\code{"Outcome recorded"}) plus the \code{\link[=quarterly_counts]{quarterly_counts()}} columns.
}
\description{
All three milestones share one zero-filled quarter range -- the union
across \code{DateScreening}, \code{DatePrevTreatmentStart}, and \code{DateOutcome} --
rather than each deriving its own from \code{\link[=quarterly_counts]{quarterly_counts()}}'s default,
since combining per-milestone ordered factors with different level sets
would otherwise error in the final \code{bind_rows()}.
}
