% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{repo_root}
\alias{repo_root}
\title{Repository root directory}
\usage{
repo_root()
}
\value{
Absolute path to the repository root.
}
\description{
Walks upward from the working directory looking for the outer Git
repository root (the directory containing \code{.git}), so paths to shared
inputs (\verb{Data/}, \verb{Documentation/}) resolve correctly whether code runs via
\code{devtools::load_all()}, \code{testthat}, or \verb{Rscript r/scripts/run.R} -- all of
which have a working directory somewhere \emph{inside} this Git repository.
Deliberately keyed on \code{.git} rather than \code{rprojroot::is_r_package()}:
the package itself lives in the \verb{r/} subdirectory (which has its own
\code{DESCRIPTION}), so a package-root criterion would stop one level too
shallow.
}
