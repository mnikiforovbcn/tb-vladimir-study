% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{raw_col_types}
\alias{raw_col_types}
\title{Column type specification for the raw dataset}
\usage{
raw_col_types()
}
\value{
A \code{readr::cols()} specification.
}
\description{
Every \code{0/1} flag and numeric code is read as \code{double} (the source export
writes some of them with a trailing \code{.0}, inconsistently across columns)
and then cast to integer in \code{\link[=load_raw]{load_raw()}} -- not declared as integer here,
since \code{readr::col_integer()} rejects \code{"12.0"}-style literals outright.
Values are kept as raw coded integers (e.g. \code{TargetGroup} stays \code{1}-\code{4});
recoding into labelled factors happens downstream in \code{derive.R}/\code{cascade.R}.
Date columns are read as character, not \code{col_date()}: a handful of rows
carry a \code{" 00:00:00"} timestamp suffix on an otherwise-plain date, which a
fixed-format date parser rejects outright; \code{\link[=load_raw]{load_raw()}} parses them itself
with a more tolerant routine.
}
\keyword{internal}
