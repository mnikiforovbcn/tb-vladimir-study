% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trends.R
\name{quarterly_counts}
\alias{quarterly_counts}
\title{Step 9 quarterly counts of a date-stamped milestone, zero-filled for empty quarters}
\usage{
quarterly_counts(
  df,
  date_col,
  group_vars = character(0),
  quarter_levels = NULL
)
}
\arguments{
\item{df}{Analysis-ready table.}

\item{date_col}{Character: name of a Date column in \code{df} (e.g.
\code{"DateScreening"}, \code{"DatePrevTreatmentStart"}, \code{"DateOutcome"}).}

\item{group_vars}{Character vector of additional grouping columns
(e.g. \code{"Source"}); the quarter range is zero-filled within every
combination of \code{group_vars} present in the data.}

\item{quarter_levels}{Optional character vector of \code{"YYYY Q#"} quarters
to use as the zero-filled range and factor levels, overriding the
range derived from \code{date_col}. Used by \code{\link[=enrollment_trends]{enrollment_trends()}} so every
milestone shares one consistent, combinable ordered factor.}
}
\value{
A tibble: \code{group_vars} columns, \code{quarter} (an ordered factor,
\code{"YYYY Q#"}), \code{n}, \code{suppressed}.
}
\description{
Quarters with zero events are kept (not dropped) so a trend line shows
true programmatic gaps (e.g., COVID-19-era disruption) rather than
missing data points. Suppression differs from \code{\link[=suppress_small_cells]{suppress_small_cells()}}:
a structural zero is not a disclosure risk and must stay visible as
\code{0}, so only \emph{nonzero} counts below \link{SMALL_CELL_THRESHOLD} are blanked.
}
