% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz.R
\name{export}
\alias{export}
\title{Export a chart or table to file}
\usage{
export(plot, path, interactive = TRUE, width = 8, height = 5)
}
\arguments{
\item{plot}{A \code{ggplot} or \code{gt_tbl} object.}

\item{path}{Output path, without extension; \code{.png}/\code{.html} is appended.}

\item{interactive}{If \code{TRUE} (the default), also write an interactive
HTML version (ignored for \code{gt_tbl}, which is always HTML).}

\item{width, height}{PNG dimensions, in inches; only used for \code{ggplot}.}
}
\value{
\code{path}, invisibly.
}
\description{
A \code{ggplot} is written as a static PNG via \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} and, if
\code{interactive = TRUE}, also as an interactive HTML page via
\code{plotly::ggplotly()} + \code{\link[htmlwidgets:saveWidget]{htmlwidgets::saveWidget()}}. A \code{gt_tbl} is
written as an HTML page via \code{\link[gt:gtsave]{gt::gtsave()}} only -- \code{gt}'s PNG export
requires a headless-browser dependency (\code{webshot2}/Chrome) that this
project does not otherwise need, so static table images are out of
scope; tables are consumed directly by the Quarto report instead.
}
