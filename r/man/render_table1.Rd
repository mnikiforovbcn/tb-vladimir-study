% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz.R
\name{render_table1}
\alias{render_table1}
\title{Render \code{\link[=table1]{table1()}} as a styled \code{gt} table, with a small-cell caveat footnote}
\usage{
render_table1(tbl1)
}
\arguments{
\item{tbl1}{A \code{gtsummary::tbl_summary} object, as returned by \code{\link[=table1]{table1()}}.}
}
\value{
A \code{gt_tbl} object.
}
\description{
\code{\link[=table1]{table1()}} is a \code{gtsummary::tbl_summary()} over the full cohort per
group, not a cascade-step numerator/denominator pair, so it is never
passed through \code{\link[=suppress_small_cells]{suppress_small_cells()}}. A small stratified cell can
still occur (e.g. a \code{RelationWithSource} level at Murom, n=380), so the
Study Plan's disclosure-control policy is surfaced here as an explicit
footnote instead of automatic suppression.
}
