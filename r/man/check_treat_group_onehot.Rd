% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc.R
\name{check_treat_group_onehot}
\alias{check_treat_group_onehot}
\title{Flag inconsistencies between \code{TreatGroup} and its one-hot flags}
\usage{
check_treat_group_onehot(df)
}
\arguments{
\item{df}{A data frame as returned by \code{\link[=load_raw]{load_raw()}}.}
}
\value{
A tibble of flagged rows with an \code{issue} column.
}
\description{
Two distinct issues are detected and labelled via \code{issue}:
\code{"sum_not_one"} (the three flags do not sum to exactly 1, ignoring \code{NA}s)
and \code{"index_mismatch"} (they sum to 1, but the flagged index does not
correspond to \code{TreatGroup}'s value). Records with \code{TreatGroup} missing
and all three flags unset (0) are not flagged -- that combination is
self-consistent ("no group assigned yet") and is surfaced instead by the
missingness audit.
}
