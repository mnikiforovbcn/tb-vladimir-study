% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc.R
\name{check_date_order}
\alias{check_date_order}
\title{Flag reversals in the expected cascade milestone date order}
\usage{
check_date_order(df)
}
\arguments{
\item{df}{A data frame as returned by \code{\link[=load_raw]{load_raw()}}.}
}
\value{
A tibble with one row per violation: \code{Source}, \code{Nomer}, \code{date_a},
\code{date_b} (the column names of the pair), and their values.
}
\description{
Compares every adjacent pair in \link{DATE_ORDER_SEQUENCE} and flags rows
where the earlier milestone date is strictly after the later one. Rows
where either date is missing are excluded (handled by the missingness
audit, not a date-order violation).
}
